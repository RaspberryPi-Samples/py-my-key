.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install rasp_access

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv rasp_access
    $ pip install rasp_access
